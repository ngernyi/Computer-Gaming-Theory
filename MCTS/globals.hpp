#ifndef GLOBALS_HPP
#define GLOBALS_HPP

#include "node.hpp"

// Declare global variables
extern node nodeArray[600000];  // Global node array
extern int nodeCounter;         // Counter for nodes
extern int currentNode;         // Current node index

#endif // GLOBALS_HPP
